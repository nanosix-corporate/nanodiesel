import {Studio} from './Studio'

export const metadata = {
  title: 'Sanity Studio',
  robots: {
    index: false,
    follow: false,
  },
}

export default function StudioPage() {
  return <Studio />
}
